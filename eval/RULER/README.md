RULER Evaluation Environment

Before run test, change model directory `$MODEL_DIR` defined in `eval/RULER/scripts/run.sh`

```bash
cd eval/RULER/
bash setup.sh

# Before run test, change model directory $MODEL_DIR defined in eval/RULER/scripts/run.sh
bash ruler_test.sh
```